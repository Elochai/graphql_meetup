en:
  graphql:
    inputs:
      user:
        sign_up_input:
          desc: Sign up user via email
          args:
            first_name: User's first name
            last_name: User's last name
            email: User's account email
            password: User's account password
            password_confirmation: User's account password confirmation
        sign_in_input:
          desc: Sign in user via email
          args:
            email: User's account email
            password: User's account password
        delete_list:
          desc:  Delete user list input
          id: List ID
        create_list:
          desc: Create user list input
          name: List name
        add_list_item:
          desc: Add List item input
          list_id: List ID
          movie_id: Movie ID
        remove_list_item:
          desc: Remove List item input
          list_id: List ID
          movie_id: Movie ID
        add_favorite_movie:
          desc: Add favorite movie input
          movie_id: Movie ID
        add_watchlist_movie:
          desc: Add watchlist movie input
          movie_id: Movie ID
        remove_favorite_movie:
          desc: Remove favorite movie input
          movie_id: Movie ID
        remove_watchlist_movie:
          desc: Remove watchlist movie input
          movie_id: Movie ID
